// Original file: proto/command_line.proto

/**
 * Wrapper to allow a list of strings in the "oneof" section_type.
 */
export interface ChunkList {
  /**
   * Wrapper to allow a list of strings in the "oneof" section_type.
   */
  chunk?: string[];
}

/**
 * Wrapper to allow a list of strings in the "oneof" section_type.
 */
export interface ChunkList__Output {
  /**
   * Wrapper to allow a list of strings in the "oneof" section_type.
   */
  chunk: string[];
}
