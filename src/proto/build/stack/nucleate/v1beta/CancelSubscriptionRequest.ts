// Original file: proto/nucleate.proto


/**
 * Username is implied in the auth headers
 */
export interface CancelSubscriptionRequest {
  /**
   * the id the sub to cancel
   */
  'id'?: (string);
}

/**
 * Username is implied in the auth headers
 */
export interface CancelSubscriptionRequest__Output {
  /**
   * the id the sub to cancel
   */
  'id': (string);
}
